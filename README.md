# PinkNotes Frontend

## Setup

1. Install:
   ```bash
   cd frontend
   npm install
   ```

2. Start:
   ```bash
   npm start
   ```

The frontend is configured to proxy API calls to http://localhost:5000
